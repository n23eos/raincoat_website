## 1. Верхняя витрина

- [x] 1.1 Заменить `selected-grid` в `#products` двумя строками плиток с плагинами Obsidian и остальными проектами; проверить наличие минимум пяти уникальных целых ссылок в каждой строке и отсутствие CTA кейсов.
- [x] 1.2 Добавить адаптивные стили плиток, отдельную бесшовную анимацию, видимый keyboard focus и fallback для reduced motion; проверить отсутствие горизонтального переполнения страницы на 320 px.

## 2. Превью и тексты

- [x] 2.1 Добавить локальные обложки для плиток без существующего подходящего снимка и связать их с разметкой; проверить HTTP 200 для каждого нового локального файла.
- [x] 2.2 Добавить RU/EN подписи строк и категорий в `i18n.js`; проверить переключение RU/EN, ссылки на сайты или подтверждённые публичные репозитории и отсутствие `case-studies.html` на главной.

## 3. Проверки

- [x] 3.1 Обновить `tests/test_site_integrity.py` под две строки витрины и выполнить `node --test tests/*.test.cjs` и `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_*.py' -v`.
- [x] 3.2 Раздать сайт через локальный HTTP-сервер и проверить витрину в браузере на desktop, 320 px, RU/EN, reduced motion, keyboard focus и отсутствие ошибок консоли.
- [x] 3.3 Выполнить `npx --yes @fission-ai/openspec@1.13.0 validate --all --strict --no-interactive`, `git diff --check`, просмотреть diff и `git status --short` перед итоговым отчётом.
